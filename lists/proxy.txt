# proxy — route through VPN
# full:    — exact host
# domain:  — host + all subdomains
# regex:   — regexp
# no prefix — same as domain:

apis.google.com
clients6.google.com
facetime.apple.com
facetimemedia.apple.com
